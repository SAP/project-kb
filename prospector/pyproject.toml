[tool.pytest.ini_options]
minversion = "6.0"
#addopts = "-ra -q --cov-report html:cov_html --cov=."
testpaths = [
    "commitdb",
    "datamodel",
    "client",
    "commit_preprocessor",
    "git",
    "api"
]

[tool.isort]
profile = "black"
multi_line_output = 3
